<h1> <a href="https://maxim-belyi.github.io/giga-school/" target="_blank">
📝Giga-school 📝</a> </h1>
<br>
# Сайт-визитка на PHP + Vite

Проект персонального сайта-визитки: объединяет классический PHP для серверного рендеринга и шаблонизации с мощным и быстрым сборщиком frontend-ассетов Vite.

---

## 🚀 Технологический стек

*   **Бэкенд:** Чистый PHP (без фреймворков) для разделения HTML на переиспользуемые шаблоны (`header`, `footer` и т.д.).
*   **Фронтенд:**
    *   JavaScript (ES6+)
    *   SCSS для написания стилей с использованием переменных, миксинов и модульной структуры.
*   **Сборщик:** [Vite](https://vitejs.dev/)
*   **Менеджеры пакетов:**
    *   NPM — для управления frontend-зависимостями.
    *   Composer — для управления PHP-зависимостями.
*   **Деплой:** (В разработке)
    *   GitHub Actions — для автоматической сборки и деплоя проекта.
    *   GitHub Pages — для хостинга готового статического сайта.

---
## 🛠️ Установка и запуск

**1. Клонируйте репозиторий:**
```bash
git clone https://github.com/Maxim-Belyi/php-business-card-Home-Work-.git
cd php-business-card-Home-Work-.git
```

**2. Установите зависимости:**
```bash
# Установка JavaScript-зависимостей
npm install

# Установка PHP-зависимостей
composer install
```

---

## 💻 Режим разработки

Для комфортной разработки необходимо запустить два параллельных процесса в двух разных окнах терминала.

**1. Запустите Vite Dev Server:**
Эта команда запустит сервер для frontend-ассетов с Hot Module Replacement (HMR).
```bash
npm run dev
```

**2. Запустите встроенный PHP-сервер:**
Эта команда запустит локальный сервер, который будет исполнять PHP-файлы.
```bash
php -S localhost:8000
```
> **Примечание:** Вы также можете использовать встроенный веб-сервер в PhpStorm, настроив его на работу с вашим PHP-интерпретатором.

После этого откройте в браузере `http://localhost:8000`, и вы увидите свой проект. Все изменения в `scss` или `js` файлах будут применяться мгновенно, без перезагрузки страницы.

---

## 📦 Сборка для продакшена

Для того чтобы собрать оптимизированные и сжатые файлы для загрузки на хостинг, выполните команду:
```bash
npm run build
```
Эта команда создаст папку `dist` со всеми необходимыми CSS, JS и другими статическими файлами, а также `manifest.json` для PHP-хелпера.

---

## 🚀 Деплой

Проект настроен на автоматическую публикацию на GitHub Pages.

Процесс деплоя запускается **автоматически при каждом пуше в ветку `main`**.
