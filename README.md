<h1> <a href="https://php-business-card-home-work-production.up.railway.app/templates/my-projects.php" target="_blank">
📝Giga-school 📝</a> </h1>
<br>
# Домашнее задание от Giga-school

Проект персонального сайта-визитки: объединяет PHP для серверного рендеринга и шаблонизации со сборщиком Vite.
Файлы проекта разбиты на компоненты (Header, Footer, Link-wrapper, constants и тд.)
---
## 🚀 Развертывание (Deployment)

Этот проект контейнеризирован с помощью **Docker** и автоматически развертывается на платформе **Railway**, что обеспечивает надежную и повторяемую среду для работы приложения.

### 🐳 Docker

Для изоляции зависимостей и создания консистентного окружения используется Docker. Файл `Dockerfile` в корне проекта описывает шаги для сборки production-ready образа.

Процесс сборки является **многоступенчатым (multi-stage)**:
1.  **Ступень `frontend-builder`**: Используется образ `node:20-alpine` для установки `npm` зависимостей и сборки фронтенд-ассетов (CSS и JS) с помощью Vite.
2.  **Финальная ступень**: Используется легковесный образ `php:8.2-apache`. В него устанавливаются PHP-зависимости через `composer`, копируется исходный код приложения и готовые ассеты из предыдущей ступени.

Такой подход позволяет финальному образу быть минималистичным и не содержать ничего лишнего (например, `node_js` или исходники фронтенда).

### ☁️ Railway (PaaS-платформа)

В качестве хостинга используется PaaS-платформа **Railway**. Она подключена напрямую к GitHub-репозиторию и обеспечивает полноценный **CI/CD пайплайн**.

**Процесс автоматического развертывания (CI/CD):**
1.  Любой `git push` в `main` ветку этого репозитория автоматически запускает новый билд на Railway.
2.  Railway читает `Dockerfile`, собирает новый Docker-образ из последних изменений в коде.
3.  После успешной сборки Railway плавно заменяет старый запущенный контейнер на новый, обновляя приложение.

### ✨ Живая версия

<h3>
<a href="https://php-business-card-home-work-production.up.railway.app/templates/my-projects.php" target="_blank">
📝Актуальная версия проекта доступна по этой ссылке 📝</a></h3>

---

В папке src находятся исходные файлы SCSS, JS для удобной поддержки и расширения 

Все перечисления вынесены в массивы и хранятся в "templates/constants.php", данные выводятся циклом

Добавлена папка home_work, в которой представлены массивы с вымышленными данными реализована функция по генерации всех возможных перестановок из этих массивов

---
## 🚀 Технологический стек

- **Бэкенд:** PHP
- **Фронтенд:**
  - JavaScript (ES6+)
  - SCSS для написания стилей с использованием переменных, миксинов и модульной структуры.
- **Сборщик:** [Vite](https://vitejs.dev/)
- **Менеджеры пакетов:**
  - NPM — для управления frontend-зависимостями.
  - Composer — для управления PHP-зависимостями.

---

## 🛠️ Установка и запуск

**1. Клонируйте репозиторий:**

```bash
git clone https://github.com/Maxim-Belyi/php-business-card-Home-Work-.git
cd php-business-card-Home-Work-
```

**2. Установите зависимости:**

```bash
# Установка JavaScript-зависимостей
npm install

# Установка PHP-зависимостей
composer install
```

## 📦 Сборка для продакшена

Для того чтобы собрать оптимизированные и сжатые файлы для загрузки на хостинг, выполните команду:

```bash
npm run build
```

Эта команда создаст папку `dist` со всеми необходимыми CSS, JS и другими статическими файлами, а также `manifest.json` для PHP-хелпера.

---
